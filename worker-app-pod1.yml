apiVersion: v1
kind: Pod
metadata:
  name: worker-voting-app
  labels:
    name: worker-voting-app
    app: demo-voting-app
spec:
  containers:
    - name: worker-voting-app
      image: docker/example-voting-app-worker

