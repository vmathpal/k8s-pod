apiVersion: v1
kind: Pod
metadata:
  name: postgres-voting-app
  labels:
    name: postgres-voting-app
    app: postgres-voting-app-pod
spec:
  containers:
    - name: postgres
      image: postgres:9.4
      ports:
        - containerPort: 5432